export class Task {
  constructor(title, description, dueDate, isPriority, isChecked, id) {
    this.title = title;
    this.description = description;
    this.dueDate = dueDate;
    this.isPriority = isPriority;
    this.isChecked = isChecked;
    this.id = id;

  }
}